"""
pattern: hashmap/ grouping
time: O(m)
space: O(m*n)

key idea: use a shared "key" fo the word ( sorted string or letter freq)
to create hashmap aka dic

code:
class Solution:
    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:
        res = defaultdict(list)
        for s in strs:
            count = [0] * 26

            for c in s:
                count[ord(c) - ord("a")] += 1
            res[tuple(count)].append(s)

        return list(res.values())


"""